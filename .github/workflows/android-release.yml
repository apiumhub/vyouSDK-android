name: Publish Release
on: workflow_dispatch
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - shell: bash
        env:
          SUPER_SECRET: ${{ secrets.SONATYPE_USERNAME }}
        run:
          echo "$SUPER_SECRET"